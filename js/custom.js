$('.ui.dropdown')
  .dropdown()
;

$('.special.cards .image').dimmer({
    on: 'hover'
  });

  $(".sidebar-menu-toggler").click(function () {
    $(".ui.sidebar")
      .sidebar("setting", "transition", "overlay")
      .sidebar("toggle");
  });

  $('.activating.element')
  .popup()
;
$('.ui.accordion')
  .accordion()
;